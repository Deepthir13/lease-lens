"""
rag.py — RAG query engine using LlamaIndex + ChromaDB + Ollama.
"""


def build_query_engine():
    """Build and return a LlamaIndex query engine backed by ChromaDB."""
    pass


def query(question: str) -> str:
    """Run a question through the RAG pipeline and return the answer."""
    pass
