"""
ingest.py — Load and chunk lease PDFs and statute text files into ChromaDB.
"""


def ingest_lease(pdf_path: str):
    """Parse a lease PDF and upsert chunks into the vector store."""
    pass


def ingest_statutes(statutes_dir: str):
    """Load pre-scraped state law text files and upsert into the vector store."""
    pass
